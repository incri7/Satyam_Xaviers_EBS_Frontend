# ── API base URL ──────────────────────────────────────────────────────────────
# Local dev: point directly at the backend server
VITE_API_BASE_URL=http://localhost:8002/

# Docker / production: route through Nginx at /api/
# VITE_API_BASE_URL=/api/
